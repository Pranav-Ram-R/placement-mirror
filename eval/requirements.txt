# Eval-only dependencies. These are not app dependencies and never go in the app
# requirements.txt. Eval scripts run on the Windows x86 dev machine only.
# Pinned to the versions installed and imported in the dev venv (Python 3.11 x64)
# on Day 1, because results depend on them.
numpy==2.4.4
torch==2.10.0
transformers==4.56.2
sounddevice==0.5.2
onnx==1.18.0
onnxruntime==1.22.1
opencv-python==4.13.0.92
matplotlib==3.11.2
qai-hub-models==0.63.0
