# Build tools for the packaged app (tools/build_app.py). Install next to requirements.txt.
# Checked on 2026-09-25 with pip download --platform win_arm64 --python-version 3.11
# --only-binary=:all:, which resolved pyinstaller-6.22.3-py3-none-win_arm64 (native ARM64
# bootloader) and pure Python wheels for altgraph 0.17.5, pefile 2024.8.26,
# pywin32-ctypes 0.2.3, pyinstaller-hooks-contrib 2026.7, packaging 26.3 and
# setuptools 84.0.0. PyInstaller is GPL-2.0-or-later with an exception that allows
# distributing programs built with it.
pyinstaller==6.22.3
pyinstaller-hooks-contrib==2026.7
