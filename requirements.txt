# App runtime dependencies. Install in the app venv (see README, Development setup).
# Each package installed alone and imported on native Windows ARM64 Python 3.11 by
# ci/check_deps.py, arm64-deps run 36101803021 (2026-09-25). Pinned to the versions
# that run installed. onnxruntime-qnn 2.6.0 bundles QAIRT 2.50.40 and pulls
# onnxruntime 1.30.0 there.
numpy==2.4.6
onnxruntime-qnn==2.6.0
fastapi==0.141.1
uvicorn==0.53.0
sounddevice==0.5.6
# WebSocket protocol for uvicorn (Task G). Pure Python wheel wsproto-1.3.2-py3-none-any,
# its one dependency h11 0.16.0 is also py3-none-any. Checked with pip download
# --platform win_arm64 --python-version 3.11 on 2026-09-25, and added to ci/check_deps.py.
wsproto==1.3.2
