# aihub

Scripts that compile and profile models on Qualcomm AI Hub hosted devices.

Each profile result is stored in benchmarks/raw/ as a Measurement with source "AI Hub hosted Snapdragon X Elite" and the AI Hub job ID.

The AI Hub API token is set with `qai-hub configure` and lives in the user profile. It is never stored in this repo.
